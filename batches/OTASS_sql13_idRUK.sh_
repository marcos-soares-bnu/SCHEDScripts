. /ixos/system/OTASS_971/bin/profile
# --- SQL_LASTID ---
sqlplus -s ecr_monitoring/ecr0401_monitoring@ECR_iasp << EOF 
SET SERVEROUTPUT ON
SET FEEDBACK OFF
DECLARE
   SQL_LASTID NUMBER;
BEGIN
   SELECT MAX(ID) INTO SQL_LASTID
     FROM ECR.ADM_JOB_PROTOCOL
    WHERE JOBNAME LIKE 'EXR3_RUK_ICC';

   DBMS_OUTPUT.PUT_LINE(SQL_LASTID);
END;
/
EXIT
EOF
